{% extends 'base.html.twig' %}

{% block title %}Prescription{% endblock %}

{% block body %}
    <h1>Prescription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prescription.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ prescription.quantite }}</td>
            </tr>
            <tr>
                <th>NombrePrise</th>
                <td>{{ prescription.nombrePrise }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ prescription.duree ? prescription.duree|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prescription_index') }}">back to list</a>

    <a href="{{ path('app_prescription_edit', {'id': prescription.id}) }}">edit</a>

    {{ include('prescription/_delete_form.html.twig') }}
{% endblock %}
