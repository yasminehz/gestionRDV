<form method="post" action="{{ path('app_assistant_delete', {'id': assistant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assistant.id) }}">
    <button class="btn">Delete</button>
</form>
